# Abhängigkeiten
node_modules/

# Echte Kampagnendaten – bleiben privat! Nur data.example/ wird versioniert.
data/

# Build-Artefakte
client/dist/
client/dist-player/
dist/
*.tsbuildinfo

# Hinweis: client/public/player-data.json wird bewusst NICHT ignoriert.
# Sie enthält ausschließlich den spoiler-gefilterten Spieler-Export und ist
# der Mechanismus, über den Inhalte für den Pages-Deploy veröffentlicht werden.

# Logs & OS-Dateien
*.log
.DS_Store
